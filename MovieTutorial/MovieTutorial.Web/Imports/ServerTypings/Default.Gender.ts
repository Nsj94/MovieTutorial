namespace MovieTutorial.Default {
    export enum Gender {
        Male = 1,
        Female = 2
    }
    Serenity.Decorators.registerEnumType(Gender, 'MovieTutorial.Default.Gender', 'Default.Gender');
}

